<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.dlsu.mobapde.werewolf">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@drawable/wolf_icon2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/wolf_icon1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ActionActivity" />
        <activity android:name=".CreateGameActivity" />
        <activity android:name=".JoinGameActivity" />
        <activity android:name=".RoleActivity" />
        <activity android:name=".WaitingPlayersActivity" />
    </application>

</manifest>